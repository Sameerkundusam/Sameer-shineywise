public class FindGreatest {
    public static void greatestInt(){
        Integer i=0,j=90,k=89,l=10;
        Integer temp=0,tem;
         if(temp<=i)
            temp = i;
         if(temp<=j)
            temp = j;
         if(temp<=k)
            temp = k;
         if(temp<=l)
            temp = l;
        System.debug('greatest = '+temp);
    }
}